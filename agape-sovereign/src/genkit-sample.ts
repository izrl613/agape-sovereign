// This sample file is intentionally lightweight so the Functions build remains stable
// when optional Genkit packages are not installed in this workspace.

export const placeholder = "genkit-sample-disabled";
